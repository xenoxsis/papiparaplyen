{
  "_comment": "Copy to cypress.env.json (gitignored) to enable tier-2 real-backend smoke tests. Run with `pnpm e2e:local` against a seeded LOCAL test DB (DB_NAME ending in _test).",
  "realBackend": true,
  "realEmail": "cypress+admin@example.com",
  "realPassword": "CypressP@ss123"
}
