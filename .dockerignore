.git
.gitignore
.next
node_modules
backend/node_modules
backend/dist

# Env files – pass secrets at runtime via `docker run -e` or a secrets manager
.env
.env.*

# Dev / docs
*.md
database/

# Cypress e2e tests (never run inside the image)
cypress/
cypress.config.ts
cypress.env.json
cypress.env.example.json
